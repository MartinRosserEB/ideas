{% extends 'base.html.twig' %}

{% block body %}
<div class="cell">Zur Verfügung stehende Ideensammlungen:</div>
<div class="cell">
    <div class="grid-x grid-padding-y">
    {% for userCollection in userCollections %}
        <div class="cell"><a href="{{ path('collection_index', {'entity': userCollection.collection.id }) }}"><strong>{{ userCollection.collection }}</strong></a></div>
    {% endfor %}
    </div>
</div>
{% endblock %}
